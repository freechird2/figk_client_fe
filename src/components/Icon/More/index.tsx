import { StyledMore } from "components/Icon/More/index.styled";

const IconMore = ({ ...rest }) => {
  return (
    <StyledMore {...rest}>
      <i />
    </StyledMore>
  );
};

export default IconMore;
